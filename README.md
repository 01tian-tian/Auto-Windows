# Auto Windows

**Auto Windows** 是一个基于视觉语言模型（VLM）的 Windows 桌面智能自动化工具。它通过截图理解当前屏幕内容，并自动执行鼠标点击、键盘输入、滚动等操作，实现对 Windows 电脑的自然语言控制。

---

## ✨ 功能特性

- **自然语言控制**：用中文或英文描述任务，AI 自动规划并执行操作
- **视觉感知**：每步操作前截取屏幕，通过视觉模型理解界面内容
- **完整操作集**：支持单击、双击、右键、拖拽、滚动、快捷键、文字输入等
- **DPI 自适应**：自动检测并适配 Windows 高 DPI/缩放设置
- **中文友好**：通过剪贴板粘贴方式支持中文输入
- **GUI 控制界面**：提供图形化控制面板，支持实时日志、任务终止
- **多步骤规划**：支持最多 100 步自动循环，直到任务完成或失败

---

## 🖥️ 截图

> GUI 控制面板支持输入指令、查看执行日志、强制终止任务。

---

## 📦 安装

### 环境要求

- Python 3.12+
- Windows 操作系统

### 安装依赖

```bash
pip install -r requirements.txt
```

`requirements.txt` 包含：

```
pyautogui>=0.9.54
Pillow>=10.0.0
pywin32>=306
pyperclip>=1.8.0
openai>=2.9.0
ttkbootstrap>=1.10.1
```

---

## 🚀 快速开始

### 方式一：命令行调用

```python
from Windows.agent import WindowsAgent, AgentConfig
from phone_agent.model import ModelConfig

model_config = ModelConfig(
    base_url="https://your-api-endpoint/v1",
    model_name="your-vlm-model",
    api_key="your-api-key",
)
agent_config = AgentConfig(max_steps=100, lang="cn")
agent = WindowsAgent(model_config=model_config, agent_config=agent_config)

result = agent.run("打开记事本，输入 Hello World")
print(result)
```

### 方式二：图形界面

```bash
python -m Windows.UI
```

在界面中输入自然语言指令，点击「执行」即可。

---

## 🗂️ 项目结构

```
Windows/
├── agent.py              # 核心 Agent 主循环，负责截图→模型→动作的循环编排
├── UI.py                 # tkinter/ttkbootstrap 图形控制界面
├── actions/
│   ├── handler.py        # 动作解析器与执行器（解析模型输出并调用桌面操作）
│   └── __init__.py
├── desktop/
│   ├── mouse.py          # 鼠标操作（点击、双击、右键、拖拽、滚动），含 DPI 适配
│   ├── keyboard.py       # 键盘操作（文字输入、快捷键、按键）
│   ├── screenshot.py     # 屏幕截图工具
│   └── __init__.py
├── config/
│   ├── prompts.py        # 系统 Prompt（中文，含操作格式说明）
│   ├── timing.py         # 操作时延配置
│   └── __init__.py
├── requirements.txt
└── __init__.py
```

---

## ⚙️ 配置说明

### AgentConfig

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `max_steps` | `100` | 最大执行步数 |
| `lang` | `"cn"` | 日志语言（`"cn"` / `"en"`） |
| `verbose` | `True` | 是否打印详细日志 |

### 坐标系统

模型输出坐标范围为 `0–999`（相对坐标），程序自动转换为屏幕实际像素并适配 DPI 缩放。

---

## 🤖 支持的动作

| 动作 | 说明 |
|------|------|
| `Tap` | 鼠标左键单击 |
| `RightClick` | 鼠标右键单击 |
| `DoubleTap` | 鼠标双击 |
| `Type` | 文字输入（支持中文） |
| `Hotkey` | 快捷键（如 `win`、`ctrl+c`） |
| `Swipe` | 鼠标拖拽 |
| `Scroll` | 滚轮滚动 |
| `Wait` | 等待指定时间 |
| `Take_over` | 请求用户手动接管 |

---

## ⚠️ 注意事项

- 本工具会直接控制鼠标和键盘，运行时请勿手动操作电脑
- 部分操作（如登录、验证码）需要用户手动接管（`Take_over`）

---

## 📄 许可证

MIT License
